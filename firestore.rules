rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissão total para qualquer usuário logado (Ideal para desenvolvimento)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
